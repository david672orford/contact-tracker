# Create a directory called "instance" and copy this configuration file
# into it as "config.py". Replace CHANGEME in the line below with a string
# of random characters. This string will be used to sign the browser
# cookie which stores the user's QR code. Don't change it once you are in
# production since that would invalidate everyone's QR code.
SECRET_KEY = b"CHANGEME"
